Create or Replace Procedure ESQUEMA.SP_Login_Insert(
--Campos de la tabla 
IN @LOGIN_CLAVE VARCHAR(10),
IN @PERSONAS_CEDULA VARCHAR(10)
)
P1:Begin

Insert into ESQUEMA.LOGIN(
LOGIN_CLAVE,
PERSONAS_CEDULA
) values (
@LOGIN_CLAVE,
@PERSONAS_CEDULA
);

End P1