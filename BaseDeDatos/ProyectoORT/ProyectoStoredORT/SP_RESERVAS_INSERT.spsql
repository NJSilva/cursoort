Create or Replace Procedure ESQUEMA.SP_Reservas_Insert(
--Campos de la tabla 
IN @PERSONAS_ID DECIMAL(5,0),
IN @LIBROS_ID DECIMAL(5,0)
)
P1:Begin

Insert into ESQUEMA.RESERVAS(
PERSONAS_ID,
LIBROS_ID
) values (
@PERSONAS_ID,
@LIBROS_ID
);

End P1