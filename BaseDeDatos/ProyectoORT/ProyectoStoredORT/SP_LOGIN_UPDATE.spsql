Create or Replace Procedure ESQUEMA.SP_Login_Update(
IN P_LOGIN_CLAVE VARCHAR(10),
IN P_PERSONAS_ID DECIMAL(5,0)
)
P1:Begin

Update ESQUEMA.LOGIN SET 
LOGIN_CLAVE=P_LOGIN_CLAVE
WHERE
PERSONAS_ID=P_PERSONAS_ID
;

End P1