Create or Replace Procedure ESQUEMA.SP_Reservas_Delete(
P_PERSONAS_ID DECIMAL(10), 
P_LIBROS_ID DECIMAL(5,0)
)
P1:Begin

Delete ESQUEMA.RESERVAS RESERVAS
WHERE RESERVAS.PERSONAS_ID = P_PERSONAS_ID
AND RESERVAS.LIBROS_ID = P_LIBROS_ID
;

End p1