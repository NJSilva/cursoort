CREATE OR REPLACE PROCEDURE ESQUEMA.SP_LOGSESIONES_OBTENER ()


DYNAMIC RESULT SETS 1

P1: BEGIN

	DECLARE C1 CURSOR WITH RETURN FOR 
	SELECT 
		* 
	FROM 
		ESQUEMA.LOGSESIONES A INNER JOIN ESQUEMA.PERSONAS B 
		ON A.PERSONAS_ID = B.PERSONAS_ID
	WHERE
		FIN IS NULL
	;
	
	OPEN C1;
	

END P1